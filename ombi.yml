rancher:
  services:
    ombi:
      image: linuxserver/ombi
      container_name: ombi
      restart: unless-stopped
      environment:
        PGID: 1000
        PUID: 1000
        TZ: America/Vancouver
      ports:
      - 3579:3579
      volumes:
      - /mnt/nfs-1/config/ombi:/config